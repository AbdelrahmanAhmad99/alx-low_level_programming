#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @dd: the character to be checked
 * Return: 1 if c is uppercase, 0 otherwise
*/
int _isupper(int dd)
{
return (dd >= 'A' && dd <= 'Z');
}
